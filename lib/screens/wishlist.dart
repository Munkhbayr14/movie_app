import 'package:flutter/material.dart';

class wishListPage extends StatelessWidget {
  const wishListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('kimchi wish'),
    );
  }
}
